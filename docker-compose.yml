services:
  reelshelf:
    build: .
    image: reelshelf:latest
    container_name: reelshelf
    restart: unless-stopped
    ports:
      - "8080:3000"
    volumes:
      - reelshelf-data:/app/data

volumes:
  reelshelf-data:
